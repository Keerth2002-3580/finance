import React, { useState } from 'react';
import { Shield, UserPlus } from 'lucide-react';
import { projectId, publicAnonKey } from '../utils/supabase/info';
const AdminSetup: React.FC = () => null;

export { AdminSetup };
